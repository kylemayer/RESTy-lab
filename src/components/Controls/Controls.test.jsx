import React from 'react';
import { render } from '@testing-library/react';
import Controls from './Controls';

describe('Controls component', () => {
  it('should display the controls component', async () => {
    render(<Controls

    />);



  });
});
